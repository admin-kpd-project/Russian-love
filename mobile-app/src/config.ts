/**
 * JS-fallback, если в приложении ничего не сохранено и нет BuildConfig (см. android/local.properties API_BASE_URL).
 * Оставьте пустой строкой — на первом запуске откроется экран ввода адреса.
 * Для эмулятора можно задать одну строку: "http://10.0.2.2:8080"
 */
export const API_URL_FALLBACK_JS = "";
