# Скопируйте в local.properties (не коммитить). Значение → BuildConfig → дефолт без экрана «Сервер».
# Рекомендуемый зашитый адрес релиза / стейджинга:
API_BASE_URL=https://dev.forruss.ru
#
# Локально замените на один из вариантов:
# API_BASE_URL=http://10.0.2.2:8000
# API_BASE_URL=http://10.0.2.2:8080
