# Скопируйте в local.properties (не коммитить) или добавьте в существующий local.properties.
# Сборка прокидывает значение в BuildConfig — приложение увидит дефолт без ввода на экране.
# Staging (как в src/config.ts API_URL_FALLBACK_JS):
# API_BASE_URL=https://dev.forruss.ru
# Эмулятор → API на ПК:
# API_BASE_URL=http://10.0.2.2:8080

API_BASE_URL=
